---
interface Props {
  href?: string;
  [key: string]: unknown;
}

const { href, ...attrs } = Astro.props;
const Tag = href ? 'a' : 'article';
---

<Tag class="card" href={href} {...attrs}>
  <slot />
</Tag>
